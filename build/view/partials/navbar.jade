header.nav_header
    .container
        nav.navbar.navbar_cus.navbar-inverse
            .container-fluid
                // Brand and toggle get grouped for better mobile display
                .navbar-header
                    button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#bs-example-navbar-collapse-1')
                        span.sr-only Toggle navigation
                        span.icon-bar
                        span.icon-bar
                        span.icon-bar
                    a.navbar-brand.about_top_log(href='/')
                        img(alt='Brand', src='img/bottom_logo.png', height='26')
                // Collect the nav links, forms, and other content for toggling
                #bs-example-navbar-collapse-1.collapse.navbar-collapse(role='navigation')
                    ul.nav.navbar-nav.navbar-right
                        if user
                            - console.log(user);
                            li.dropdown
                                a.dropdown-toggle(href='javascript:void(0)', data-toggle='dropdown')
                                    | #{user.name || user.email}
                                    b.caret
                                ul.dropdown-menu
                                    li
                                        a(href='/logout') 退出
                        else
                            li
                                a(href='#', data-toggle='modal', data-target='#Login') 登录
                            li
                                a.nav_reg_btn(href='/signup1') 立即注册